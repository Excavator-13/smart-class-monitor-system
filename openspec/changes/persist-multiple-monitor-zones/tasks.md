# Tasks

- [x] 1. Audit the existing frontend zone confirmation/deletion flow and SpringBoot AI reload ownership.
- [x] 2. Add frontend service functions for `POST /zones` and `DELETE /zones/{id}`.
- [x] 3. Derive confirmed rectangles from persisted zones for the active stream.
- [x] 4. Persist one draft per confirmation while retaining existing confirmed zones.
- [x] 5. Render multiple zones and add a per-zone deletion list in normal video.
- [x] 6. Apply the same interaction to full-screen video.
- [x] 7. Match phone detections against all confirmed zones.
- [x] 8. Run frontend build verification and validate the OpenSpec artifacts manually because the `openspec` CLI is unavailable in this environment.
