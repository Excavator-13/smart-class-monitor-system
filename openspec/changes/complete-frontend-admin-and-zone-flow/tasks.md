# Tasks

- [x] Audit AI zone rendering, zone DTOs, user APIs, mock/local storage, and HTTP tests.
- [x] Remove persisted Vue zone overlays and keep draft-only overlays in normal/full-screen views.
- [x] Add draft zone name/type controls and submit the selected type.
- [x] Add `zone_type` to the backend update DTO/service.
- [x] Increase monitor alert tracking height.
- [x] Add frontend user adapters, admin-only navigation/page, and role update handling.
- [x] Confirm non-report mock/local-only behavior cannot silently affect production mode.
- [x] Replace `api-test.http` with a concise contract-aligned suite.
- [ ] Build/test frontend and backend, then manually validate OpenSpec because the CLI is unavailable. Frontend build and browser checks pass; backend tests require Maven, which is unavailable on this machine.
